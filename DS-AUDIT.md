# DS-AUDIT — «Проект Уралмаш»

Аудит фактических значений в `Desktop\Проект Уралмаш` перед сборкой дизайн-системы.
Источники: `Дробилки КМД-КСД.dc.html` (5518 строк, 326 методов), `Storybook.dc.html`, `styles.css`, `_ds/industry-*/`.

**Ни один файл проекта не изменён.** Отчёт носит измерительный характер; решения — за автором.

---

## Сводка

| Категория | Уникальных значений | Вхождений | Реально нужно |
|---|---|---|---|
| Spacing | 74 (JS) + разброс в CSS | 517 | 6–7 |
| Radius | 6 | 29 | 3 + full |
| Цвета (сырые hex вне токенов) | 38 | 92 | 0 |
| Кегль (fontSize) | 18 | 199 | 5–6 ролей |
| Насыщенность | 4 | 71 | 3 |
| Интерлиньяж | 6 | 16 | связать с кеглем |
| Фикс. высоты | 48 | 197 | 2–3 (control sizes) |
| Ширины | 85 | 298 | шкала контейнеров |
| Тени | 3 токена | 20 | 3 (уже ок) |

**Главный вывод:** токены в проекте есть и местами используются (593 обращения к `var(--*)`), но шкала spacing спроектирована так, что ей физически неудобно пользоваться — поэтому её обходят. Это не разгильдяйство исполнителя, а дефект самой шкалы.

---

## 1. Spacing — корень проблемы

### Что в токенах

```
--space-1: 3.4px    --space-2: 6.8px    --space-3: 10.2px
--space-4: 13.6px   --space-6: 20.4px   --space-8: 27.2px
```

Шкала выведена как `4px × 0.85` (density-множитель темы Industry). Значения **дробные и не лежат ни на 4px, ни на 8px сетке**.

### Что в коде фактически

| Значение | Частота | Ближайший токен | Расхождение |
|---|---|---|---|
| 10 | 52 | `--space-3` (10.2) | 0.2px |
| 6 | 46 | `--space-2` (6.8) | 0.8px |
| 8 | 45 | — (между 6.8 и 10.2) | нет токена |
| 16 | 41 | — (между 13.6 и 20.4) | нет токена |
| 14 | 41 | `--space-4` (13.6) | 0.4px |
| 12 | 35 | — | нет токена |
| 20 | 26 | `--space-6` (20.4) | 0.4px |
| 4 | 22 | — (между 3.4 и 6.8) | нет токена |
| 2 | 14 | — | нет токена |
| 28 | 13 | `--space-8` (27.2) | 0.8px |
| 7 | 13 | — | нет токена |
| 3 | 9 | `--space-1` (3.4) | 0.4px |

Плюс хвост: 18, 24, 34, 5, 22, 30, 33, 40 и составные строки (`'7px 8px'`, `'8px 10px'`, `'20px 24px'`, `'7px 9px'`, `'4px 2px'`, `'6px 10px'` — 29 уникальных пар).

**Диагноз.** Четыре самых частотных значения (8, 16, 12, 4 — суммарно 143 вхождения) **не имеют токена вообще**. Остальные отличаются от токена на 0.2–0.8px — визуально неразличимо, но заставляет писать число руками. Токены `--space-*` использованы всего 42 раза против ~475 сырых чисел.

Итог: шкала не работает как ограничение. Разработчик пишет `padding: 10`, потому что `var(--space-3)` даёт 10.2 и выглядит как лишняя работа ради того же результата.

### Кандидат

Сетка 4px, целые значения, 7 ступеней:

| Роль | px | Покрывает из факта |
|---|---|---|
| `2xs` | 2 | 2, 3 |
| `xs` | 4 | 4, 5 |
| `sm` | 8 | 6, 7, 8 |
| `md` | 12 | 10, 12, 14 |
| `lg` | 16 | 16, 18 |
| `xl` | 24 | 20, 22, 24 |
| `2xl` | 32 | 28, 30, 34 |

Это **требует подтверждения**: слияние 10/12/14 в один `md` = 12px изменит плотность части экранов. См. вопрос 1.

---

## 2. Типографика

Отдельных шкал кегля и интерлиньяжа нет — они собираются заново в каждом месте.

| fontSize | Частота | Комментарий |
|---|---|---|
| **12.5** | 72 | доминирующий размер интерфейса, дробный |
| 13 | 28 | |
| **11.5** | 21 | дробный |
| 12 | 17 | конфликтует с 12.5 |
| 16 | 14 | |
| 11 | 9 | конфликтует с 11.5 |
| 14 | 7 | базовый по `styles.css`, но в коде почти не встречается |
| 13.5 | 4 | |
| 8, 9.5, 10, 10.5, 15, 18, 22, 24 | 1–4 | хвост |

**Красный флаг:** пары 11/11.5, 12/12.5, 13/13.5 — это не иерархия, а дрейф. Различие в полпикселя не читается, но плодит по три варианта одной и той же роли.

Интерлиньяж: 1.2, 1.3, 1.35, 1.4, 1.45, 1.5, 1.55, 1.6 — восемь значений, ни одно не привязано к кеглю.

Насыщенность: 600 (47), 700 (15), 400 (8), 500 (1). Кандидат — оставить 400/600/700, значение 500 свести.

**Кандидат ролей** (size / line-height / weight):

| Роль | Размер | Заменяет |
|---|---|---|
| `caption` | 11 / 1.4 / 400 | 8, 9.5, 10, 10.5, 11, 11.5 |
| `body-sm` | 12.5 / 1.4 / 400 | 12, 12.5 |
| `body` | 13 / 1.45 / 400 | 13, 13.5, 14 |
| `body-lg` | 15 / 1.5 / 400 | 15, 16 |
| `heading-sm` | 16 / 1.3 / 600 | 16, 18 |
| `heading-md` | 22 / 1.2 / 600 | 22, 24 |
| `heading-lg` | 32 / 1.15 / 700 | 32, 34 |

`12.5` намеренно сохранён — это самый частотный размер, его нормализация до 12 или 13 изменит вид всего приложения. См. вопрос 2.

**Решение автора (принято позже, отменяет кандидата выше).** Шкала задана не от измеренного разброса, а от назначения роли:

| Роль | Размер | Назначение |
|---|---|---|
| `headingLg` | 32 / 1.15 / 700 | Заголовок страницы |
| `headingMd` | 24 / 1.2 / 600 | Заголовок секции |
| `headingSm` | 16 / 1.3 / 600 | Заголовок карточки |
| `bodyLg` | 16 / 1.5 / 400 | Вводный текст |
| `body` | 14 / 1.45 / 400 | Основной текст форм |
| `bodySm` | 12 / 1.4 / 400 | Плотные области: таблицы, меню |
| `label` | 12 / 1.35 / 600 | Подпись поля |
| `caption` | 12 / 1.4 / 400 | Служебная подпись, единицы измерения |

`caption` и `bodySm` совпали по значению. Решение автора — размер один, начертания оставляем: обе роли сохранены, но ссылаются на одну тройку, поэтому разойтись не могут.

---

## 3. Цвет

### Что работает хорошо

Ramps 100–900 для neutral и accent — сгенерированы в OKLCH на общей шкале светлоты. Это грамотно, и используется активно: `--color-surface` (88), `--color-divider` (85), `--color-text` (58), `--color-accent` (51), `--color-neutral-600` (46).

### Дыры

**1. Нет статусных ролей.** В токенах отсутствуют `danger`, `success`, `warning`. В коде это выглядит так:

```js
color: 'var(--color-danger-700, #b3261e)'   // ×7
```

Токена `--color-danger-700` не существует ни в `styles.css`, ни в `_ds/`. **Fallback срабатывает всегда** — то есть цвет ошибки фактически захардкожен, и написан он так, будто токен есть. Это худший вид расхождения: код выглядит системным, а не является.

**2. `#fff` — 42 вхождения.** Прямое белое вместо `--color-bg` / `--color-surface`. В светлой теме разница незаметна, при попытке сделать тёмную тему — сломается всё разом.

**3. Палитра тегов существует в двух несовместимых версиях.**

В приложении (`Дробилки КМД-КСД.dc.html`) — 6 ключей на `color-mix` + OKLCH:
```js
TAG_PALETTE = [ steel, sage, amber, clay, violet, slate ]
```

В сторибуке (`Storybook.dc.html`) те же теги — сырыми хексами:
```html
background:#f6e3c8   background:#dbe6f0   background:#e4e4e4
background:#dcefd6   background:#f0dbe0   background:#e2dcf0
```

Сторибук, который должен быть источником правды, уже разошёлся с приложением. Ни один из этих хексов не выводится из `TAG_PALETTE`.

**4. `--color-accent-2-*`** — по документации самой системы это «машинная заглушка», дублирующая accent. Девять неиспользуемых токенов, которые выглядят как настоящая роль.

---

## 4. Radius

Здесь почти порядок: `--radius-sm/md/lg` = 2/4/7px.

Фактически: `4` и `var(--radius-md)` (23 вхождения), `0` (18), `999`/`50%` (5), `2` (1). То есть реально живут три значения: **0 (blueprint-объекты), 4 (контролы), full (аватары/бейджи степпера)**.

`--radius-lg` (7px) использован 1 раз. `--radius-sm` (2px) — 1 раз. Кандидат: свести к `none | md (4) | full`, sm/lg удалить.

---

## 5. Размеры контролов

| Высота | Частота | Где |
|---|---|---|
| **38** | 38 | `.input`, `.btn` — основной размер |
| **34** | 15 | `.btn-icon`, компактные кнопки |
| 33 | 5 | футер выпадающего меню |
| 40 | 6 | ? |
| 30, 36, 44 | 4+2+2 | хвост |

Три размера в диапазоне 33–40 делают одно и то же. Кандидат — две ступени: `md = 36`, `sm = 32`, и одна `lg = 44` при необходимости. Требует подтверждения — см. вопрос 3.

**Ширины модалок — 11 разных значений:**
```
min(560px,85vh)  min(620px,85vh)  min(640px,88vh)  min(660px,82vh)
min(680px,94vw)  min(720px,88vh)  min(760px,94vw)  min(1000px,94vw)
min(1100px,96vw) min(1200px,96vw) min(1300px,96vw)
```
Плюс vh-ограничения вперемешку с vw (`85vh` против `94vw` в одной роли). Кандидат — 3 размера: `sm 560 / md 720 / lg 1100`.

**Решение автора:** две ступени — `sm min(560px, 94vw)` и `lg min(1280px, 96vw)`. `1280` вместо `1100`: чётное значение, делится пополам без остатка.

---

## 6. Дубли компонентов

| Роль | Реализации | Различия | Рекомендация |
|---|---|---|---|
| **Модалка** | 23 × `.dialog`, но `.dialog-body` только 6 | 17 модалок собирают тело вручную; отступ заголовка `marginBottom` = 12 / 14 / отсутствует; футер = `gap 10` / `gap 6` / `gap 4`, у трёх есть `borderTop`, у остальных нет | Один `Modal` со слотами `Header / Content / Footer`. Анатомия зашита, содержимое — снаружи |
| **Футер модалки** | 5 вариантов | `justifyContent: flex-end` + произвольные `gap`/`marginTop`/`paddingTop`/`borderTop` | `Modal.Footer`, отступы и разделитель — внутри |
| **Пустое состояние** | 5 × «Ничего не найдено.» | у двух `padding: '20px'` без рамки, у двух — с `border` + `borderRadius`, у одного просто текст | `EmptyState` с фиксированной анатомией |
| **Прочерк «—»** | 56 вхождений | `text-muted` + `fontSize` то 12, то 12.5, то без размера | Компонент `Empty`/`Placeholder` либо правило в `Text` |
| **Тег/чип** | 2 системы | `TAG_PALETTE` в приложении vs сырые хексы в сторибуке | Один `Tag` на семантических токенах; сторибук генерировать из него |
| **Кнопка-иконка** | `btn btn-icon btn-ghost` (26), `btn btn-ghost btn-icon` (2), `btn btn-icon btn-ghost header-cell` (7) | порядок классов и примеси | `IconButton` с пропами |
| **Поле ввода** | `.input` (74) + `.field` (37) + `.float-field` (2) | плавающая подпись только у двух полей — остальные без неё | Решить: `Field` с `label` всегда, или два явных варианта |
| **Выпадающий список** | `card elev-md sel-menu`, `card elev-md`, `menu-row` | меню собирается из карточки заново в каждом месте | `Menu` / `SelectBox` как компонент |

---

## 7. Красные флаги

1. **`var(--color-danger-700, #b3261e)`** — код имитирует использование токена, которого нет. Семь мест. Самая опасная находка: выглядит правильно при чтении, не является правильным при исполнении.

2. **Шкала spacing не совпадает с потребностью.** Четыре самых частотных значения не имеют токена. Систему обходят не по небрежности, а потому что она не покрывает реальные случаи.

3. **Сторибук разошёлся с приложением** по палитре тегов. Документация, которая врёт, хуже отсутствующей.

4. **Полупиксельные пары кегля** (11/11.5, 12/12.5, 13/13.5) — иерархия, которой не существует визуально.

5. **`#fff` × 42** — тёмная тема сейчас невозможна в принципе.

6. **Монолит на 326 методов в одном файле** — нет границ компонентов, поэтому любое «одинаковое» решение приходится повторять руками. Это техническая причина повторяющихся UI-багов, о которых идёт речь в задаче.

7. **Инлайн-стили как основной способ вёрстки** — ~1000 стилевых объектов в JS. Ни линтер, ни типы их не контролируют.

---

## Вопросы к решению

**1. Spacing: сводить 10/12/14 в один шаг 12px?**
- *Да* — чистая шкала на сетке 4px, но плотность части экранов заметно изменится (±2px в сотнях мест).
- *Нет, сохранить дробную сетку 3.4px* — вид 1:1, но шкалой по-прежнему неудобно пользоваться, и проблема вернётся.
- *Компромисс:* сетка 4px, но с шагом `10` вместо `12` в роли `md` — ближе к текущему виду.

**2. Кегль: что делать с 12.5px (72 вхождения)?**
- *Оставить как есть* — роль `body-sm: 12.5`, вид не меняется, но шкала остаётся дробной.
- *Свести к 13* — чище, весь интерфейс станет чуть крупнее.
- *Свести к 12* — чище, интерфейс станет плотнее.

**3. Высота контролов: 38 или 36?**
Сейчас 38 (основной) и 34 (компактный). На сетке 4px это 36 и 32. Менять или зафиксировать 38/34 как есть?

**4. Статусные цвета: какие роли нужны?**
Сейчас существует только `danger` (и то хардкодом). Нужны ли `success` и `warning` как полноценные роли со шкалой 100–900, или достаточно `danger`?

**5. Палитра тегов: приложение или сторибук?**
Версии несовместимы. Считаем правильной `TAG_PALETTE` из приложения (6 ключей на OKLCH) и переносим её в токены?

**6. `--color-accent-2-*` — удалять?**
Девять токенов-заглушек, дублирующих accent. Удалить или оставить задел на вторую акцентную роль?

**7. Ширины модалок: три размера (560 / 720 / 1100) достаточно?**
Сейчас одиннадцать. Есть ли модалка, которой действительно нужен нестандартный размер?

**8. Тёмная тема — в планах?**
От этого зависит, насколько жёстко выкорчёвывать `#fff` и требовать семантику вместо примитивов на первом же шаге.

---

## Следующий шаг

`ds-tokens` — но **только после ответов на вопросы выше**. Канонические значения должен подтвердить автор, а не угадать аудит.

---

# Пересмотр 20.08.2026 — переход на визуальный язык Geist

Этот раздел дописан после аудита и отменяет часть его выводов. Отменяет
**сознательно**: аудит измерял «Проект Уралмаш» и отвечал на вопрос «какие
значения там фактически есть». Решение перейти на Geist — ответ на другой
вопрос, и мерить его тем же аудитом нельзя.

## Что отменено

**Радиусы: `none / md 4 / full` → `none / sm 6 / md 12 / full`.**

Аудит (раздел 4) свёл разброс к двум значениям и обосновал прямые углы у
карточек: «Industry рисует их как чертёжные объекты». Это была не экономия
шкалы, а утверждение о языке — и в `tokens/README.md` оно так и называлось
основой языка системы.

Теперь язык другой. Ступень выбирается по роли объекта, а не по размеру:
`sm` — контрол, `md` — оболочка. Прямой угол сохранён ровно там, где он
означает примыкание, а не характер, — ячейки шапки на своей шкале `chrome`.

**Акцент: `#5980a6` → верхняя ступень нейтральной лестницы.**

Аудит взял стальной синий из приложения. В новом языке главное действие
выделяется контрастом, а не оттенком: на светлой теме почти чёрный, на тёмной
белый. Синий не исчез — он перешёл в роль сообщения: ссылка, пояснение,
кольцо фокуса.

Кольцо фокуса при этом перестало быть цветом акцента и стало синим. Это
вынужденно и осознанно: чёрное кольцо на чёрной кнопке не видно, а фокус
обязан читаться на любом контроле.

**Гарнитуры: `Barlow` / `Barlow Condensed` → `Geist` / `Geist Mono`.**

Цена названа прямо: `bodyLg` и `headingSm` совпадают по кеглю (16), и прежде
их разводила гарнитура. Теперь различие несут только вес и интерлиньяж —
`400 / 1.5` против `600 / 1.3`. Различие стало тоньше; если окажется
недостаточным, чинить надо шкалу, а не места применения.

Взамен появилось то, чего у прежней пары не было: `Geist Mono` для числовых
колонок характеристик. В таблице дробилок разряды встают столбиком — 1750 под
900, а не левее его. Для инженерных данных это читаемость, а не оформление.

**Лестницы: 9 ступеней → 10, с назначением у диапазона.**

`100–300` фон компонента, `400–600` границы, `700–800` контрастная заливка,
`900–1000` текст. Роли выбирают ступень по договору, а не на глаз.

Тёмная тема получила собственную лестницу `neutralDark`. Прежнее правило
«лестницы не меняются никогда» на практике уже нарушалось: тёмные роли
подставляли сырые `#131516` прямо в `tokens.css`, минуя примитивы. Отдельная
лестница возвращает тёмную тему под общее правило.

## Что НЕ взято у Geist, и почему

**Обращение к ступени из компонента** — `var(--ds-gray-100)` как фон,
`var(--ds-gray-1000)` как текст. Geist устроен так, и это его право.

Нам нельзя. Раздел 3 этого аудита нашёл `var(--color-danger-700, #b3261e)`
в семи местах: токена не существовало, срабатывал хардкод-fallback, и код
выглядел системным, не будучи им. Система строилась в том числе против этого.
Ступени по-прежнему видит только `semantic.ts`; компонент знает роли.

**«Материалы» с тенью и бордером разом.** Правило «тень ИЛИ бордер» осталось
в силе, единственное исключение — модалка, как и было оговорено. У Geist слои
разделяются почти без заливки, одним хайрлайном, и правило этому не мешает.

## Точность соответствия

Значения выведены в логике Geist, а не скопированы. Числовых значений палитры
Vercel в публичной документации не публикует — страница рисует их скриптом.
Совпадения по устройству ожидать стоит, по конкретным шагам — нет. Заявлять
«у нас палитра Geist» неверно; верно — «у нас палитра, устроенная как Geist».
